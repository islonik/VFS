package org.vfs.server.command;

import org.junit.Assert;
import org.junit.Test;
import org.vfs.core.command.Command;
import org.vfs.server.command.impl.*;

import java.util.HashMap;

/**
 * @author Lipatov Nikita
 */
public class ServerMappingTest
{/*
    @Test
    public void testGettingCommands()
    {
        ServerMapping serverMapping = new ServerMapping();
        HashMap<String, Command> commands = serverMapping.getMapping();

        Assert.assertEquals(ChangeDirectoryCommand.class.getCanonicalName(), commands.get("cd").getClass().getCanonicalName());
        Assert.assertEquals(ConnectCommand.class.getCanonicalName(),         commands.get("connect").getClass().getCanonicalName());
        Assert.assertEquals(CopyCommand.class.getCanonicalName(),            commands.get("copy").getClass().getCanonicalName());
        Assert.assertEquals(LockCommand.class.getCanonicalName(),            commands.get("lock").getClass().getCanonicalName());
        Assert.assertEquals(MakeDirectoryCommand.class.getCanonicalName(),   commands.get("mkdir").getClass().getCanonicalName());
        Assert.assertEquals(MakeFileCommand.class.getCanonicalName(),        commands.get("mkfile").getClass().getCanonicalName());
        Assert.assertEquals(MoveDirectoryCommand.class.getCanonicalName(),   commands.get("move").getClass().getCanonicalName());
        Assert.assertEquals(PrintCommand.class.getCanonicalName(),           commands.get("print").getClass().getCanonicalName());
        Assert.assertEquals(QuitCommand.class.getCanonicalName(),            commands.get("quit").getClass().getCanonicalName());
        Assert.assertEquals(RemoveCommand.class.getCanonicalName(),          commands.get("rm").getClass().getCanonicalName());
        Assert.assertEquals(UnlockCommand.class.getCanonicalName(),          commands.get("unlock").getClass().getCanonicalName());

    }*/
}
